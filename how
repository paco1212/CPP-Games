g How to use GITHUB 

# use git remote add <variable> <link> to get access to the repository # people usually use “origin” as a variable

#use mu to get the files from the master repository

# use git status to see what the status of repository

#use git add <filename> to essentially get ready to push it back into the repository

# use git status again to see what the status of repository

# use git commit to get ready to push
	use git commit -m <message> to type in a message

# use git push <variable> master to push all content to the master repository; variable need to match the one you used to get the content.